/'
 '  UML include file for DcsUI enums
 '/

@startuml

enum Orientation {
    HORIZONTAL,
    VERTICAL
}

enum PositionType {
    LEFT,
    RIGHT,
    TOP,
    BOTTOM
}

enum PolarAxisType {
    MAGNITUDE,
    ANGLE
}

enum ColorGradientType {
    RGB,
    HSV
}

@enduml
