!ifdef SHOW_ENUMS
enum ConfigFormat {
    OPTIONS,
    INI,
    JSON,
    XML,
    MIXED,
    INVALID
}

enum ConfigEntry {
    NAMESPACE,
    FORMAT
}

enum FactoryError {
    TYPE_NOT_FOUND
}

enum LogLevel {
    LEVEL_TRACE
}

enum MessageType {
    UNKNOWN,
    ALERT,
    ERROR,
    INFO,
    WARNING
}
!endif
